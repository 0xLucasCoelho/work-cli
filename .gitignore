/target
# Cargo.lock IS committed: this workspace ships a binary.

# Personal image recipes are local-only (not part of the OSS project).
/Dockerfile.personal

# Local git worktrees (e.g. .worktrees/feat-foo).
/.worktrees/
